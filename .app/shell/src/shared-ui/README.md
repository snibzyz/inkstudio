# Shared UI

Put reusable UI components here after copying/adapting them from the existing website UI library.

Reference source for style direction:

- `.example/ui`
- `shared/theme/colorConfig.ts`

Suggested structure:

- `components/`
- `tokens/`
- `themes/`
- `icons/`
- `index.ts`
